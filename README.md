# Eklavya Gogia — Portfolio

React + Vite portfolio prepared for GitHub Pages and the custom domain `ekalvyagogia.me`.

## Deploy

Push the repository to GitHub and set **Settings → Pages → Source** to **GitHub Actions**. Every push to `main` will build and deploy the site automatically.
